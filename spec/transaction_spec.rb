require_relative('../lib/transaction.rb')

describe 'Transaction' do
  describe '#new' do
    it 'responds to #new with three arguments: (transaction amount, date, balance)'

    it 'allows access to .amount'

    it 'allows access to .date'

    it 'allows access to .balance'

    it 'balance is zero by default'
  end
end
